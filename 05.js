/* Dado o array fruits = ['apple', 'banana', 'orange', 'kiwi'], utilize o método forEach para exibir cada fruta no console. */

let frutas = ['apple', 'banana', 'orange', 'kiwi']

frutas.forEach((fruta,posicao,possibi)=>{
    console.log(`Sua fruta é: ${fruta}`);
    console.log(`--------------------------------`);
})