/*Dado o array numeros = [1, 2, 3, 4, 5], utilize o método map para criar um novo array contendo o dobro de cada elemento. */

let numeros = [1, 2, 3, 4, 5]
let novo = numeros.map((numeros)=>{
    return numeros * 2
})
console.log(novo);
